// import 'hadiah.dart';
// import 'kendaraan.dart';
// import 'message.dart';
// import 'calculator.dart';

void main() {
  // var truk = Truk(8);
  // print(truk.roda);

  // var sekarang = DateTime.now();
  // print(sekarang);

  // var kendaraan = Kendaraan.fromJson({"roda": 4});
  // print(kendaraan);
  // kendaraan.isiBensin(10);
  // kendaraan.maju();
  // kendaraan.pabrikan;

  // print("=" * 10);

  // var mobil = Mobil();

  /// Constructor are not inherited
  // var mobil = Mobil.fromJson();

  // print(mobil);
  // print(mobil.roda);
  // mobil.isiBahanBakar(10);
  // mobil.maju();

  // var bus = Bus();
  // print(bus);

  // final textMsg = TextMessage('Alice', 'Hello there!', DateTime.now());
  // textMsg.display();
  // final imageMsg = ImageMessage('Bob', 'image.jpg', DateTime.now());
  // imageMsg.display();

  // List<Kendaraan> kendaraanList = [
  //   Kendaraan(4, 4),
  //   Kendaraan(2, 0),
  // ];
  // print(kendaraanList);

  // var hadiah = Hadiah("1");
  // hadiah.tipeDataIsi();
}
